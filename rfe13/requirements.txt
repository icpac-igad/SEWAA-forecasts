# cGAN-RFE operational forecast — runtime dependencies.
# Versions are those validated on the deployment host (RTX 5000 Ada, Python 3.11).
# For GPU, install the matching CUDA wheels of tensorflow (the run scripts point
# LD_LIBRARY_PATH at tensorflow's bundled nvidia-* libs automatically).

tensorflow==2.21.0
numpy==2.5.0
xarray==2026.4.0
netCDF4==1.7.4
h5py==3.14.0
PyYAML==6.0.3
scipy==1.18.0
properscoring==0.1
pandas==3.0.3

# --- optional: only needed for the plotting/eval utilities, NOT for forecasting ---
# matplotlib
# cartopy
# seaborn
# scikit-learn
# trackio
